plugins {
    id 'com.android.application'
}

android {
    compileSdkVersion 32

    defaultConfig {
        applicationId "com.lavadip.searcher"
        minSdkVersion 19
        targetSdkVersion 30
    }

    namespace 'com.lavadip.searchertest'
}